class Solution:
    def searchInsert(self, arr, target):
        l, r = 0, len(arr) - 1
        while l <= r:
            m = (l + r) // 2
            if arr[m] < target:
                l = m + 1
            else:
                r = m - 1
        return l
